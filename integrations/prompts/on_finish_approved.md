{{if .change_url}}The review of {{if eq .forge "gitlab"}}merge request !{{.change_number}}{{else}}pull request #{{.change_number}}{{end}} ({{.change_url}}) finished with no open comments. There is nothing to push, and no files need changing.{{else if eq .total_count 0}}Review approved with no comments — no changes requested.{{else}}Review approved. All comments are resolved — proceed with implementation.{{end}}
